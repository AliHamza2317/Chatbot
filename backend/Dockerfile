# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build chitfund-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t chitfund-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system chitfund-backend && \
          adduser --system -G chitfund-backend chitfund-backend

COPY dist/chitfund-backend chitfund-backend/
RUN chown -R chitfund-backend:chitfund-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix chitfund-backend --omit=dev -f install

CMD [ "node", "chitfund-backend" ]
